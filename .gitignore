# Secrets — NEVER commit
.env

# Python environment
.venv/
__pycache__/
*.pyc

# Local data stores (large / regenerable) — do not commit
*.duckdb
*.duckdb.wal

# Poller run logs
poll.log
nohup.out

# Jupyter
.ipynb_checkpoints/

# dbt
dbt/target/
dbt/logs/
dbt/dbt_packages/
dbt/.user.yml
